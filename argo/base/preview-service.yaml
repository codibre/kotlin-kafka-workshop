apiVersion: v1
kind: Service
metadata:
  labels:
    app: rollout-todo-list-service
  name: preview-todo-list-service
  namespace: todo-list
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: rollout-todo-list-service
  type: NodePort
